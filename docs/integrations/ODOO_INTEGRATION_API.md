# Odoo Integration API

**Product:** Workorder Generator

**Audience:** Odoo integration developers and deployment engineers

**API version:** v1

**Document version:** 2.0

**Last verified:** July 29, 2026

**Status:** Implemented and locally acceptance-tested

## 1. Purpose

This API allows a trusted Odoo server or integration worker to:

1. list office-approved workorders waiting for Odoo;
2. read one integration-safe workorder payload;
3. report that Odoo needs more information; and
4. record the Odoo service-order identity after a successful handoff.

Use only the versioned service API:

```text
https://<product-host>/api/integrations/odoo/v1
```

> Do not use browser cookies or `/api/surveillance/*` routes. Those routes belong to the first-party user interface and are not an external integration contract.

The machine credential is scoped to one company. A client can never list or read another company's workorders.

## 2. Integration workflow

```text
Office approves workorder
        |
        v
Workorder lifecycle = closed
        |
        v
Odoo worker lists status=pending
        |
        v
Odoo worker reads full payload
        |
        +--> Mapping incomplete
        |       |
        |       v
        |    PUT status=missing_info
        |    Office receives attention state
        |
        +--> Mapping complete
                |
                v
             Create/update Odoo record
                |
                v
             PUT status=entered
             Product stores Odoo identity
```

### Product lifecycle

```text
open
  -> accepted
  -> in_progress
  -> mechanic_done
  -> closed
  -> odoo_entered
```

Only `closed` and `odoo_entered` workorders are visible through this API.

### Odoo handoff state

| State | Meaning |
| --- | --- |
| `not_entered` | Office-approved workorder is ready for Odoo. |
| `missing_info` | Odoo is blocked and office attention is required. |
| `entered` | The Odoo record identity was stored successfully. |

## 3. Authentication

Every request requires a company-scoped machine token:

```http
Authorization: Bearer <integration-token>
Accept: application/json
```

Tokens use this format:

```text
wgi_<lookup-prefix>.<secret>
```

The token is generated by an authorized Admin in **Settings → Integrations → Machine clients**.

- The raw token is displayed once.
- Only a SHA-256 hash and lookup prefix are stored.
- Store the raw token in the Odoo deployment secret manager.
- Never place the token in source control, tickets, screenshots, or logs.
- Revoke the client immediately if the token is exposed.

Required scopes:

| Scope | Permission |
| --- | --- |
| `workorders:read` | List and read eligible workorders. |
| `workorders:write` | Record `missing_info` or `entered` results. |

## 4. Quick start

Set deployment secrets:

```bash
export WORKORDER_API_BASE_URL="https://<product-host>/api/integrations/odoo/v1"
export WORKORDER_INTEGRATION_TOKEN="<secret-manager-value>"
```

List pending workorders:

```bash
curl --fail-with-body \
  --header "Authorization: Bearer ${WORKORDER_INTEGRATION_TOKEN}" \
  --header "Accept: application/json" \
  "${WORKORDER_API_BASE_URL}/workorders?status=pending&limit=100"
```

Read one workorder:

```bash
curl --fail-with-body \
  --header "Authorization: Bearer ${WORKORDER_INTEGRATION_TOKEN}" \
  --header "Accept: application/json" \
  "${WORKORDER_API_BASE_URL}/workorders/<workorder-uuid>"
```

Record a successful Odoo handoff:

```bash
curl --fail-with-body \
  --request PUT \
  --header "Authorization: Bearer ${WORKORDER_INTEGRATION_TOKEN}" \
  --header "Content-Type: application/json" \
  --header "Idempotency-Key: <stable-unique-key>" \
  --data '{
    "status": "entered",
    "serviceOrderNo": "SO-10482",
    "externalId": "odoo:maintenance.request:7812",
    "note": "Created by Odoo integration"
  }' \
  "${WORKORDER_API_BASE_URL}/workorders/<workorder-uuid>/result"
```

## 5. List workorders

```http
GET /workorders?status=pending&limit=100&cursor=<opaque>
```

### Query parameters

| Parameter | Required | Values | Default |
| --- | --- | --- | --- |
| `status` | No | `pending`, `missing_info`, `entered` | `pending` |
| `limit` | No | Integer from 1 to 200 | `100` |
| `cursor` | No | Opaque cursor returned by the preceding page | None |

Do not parse, edit, or construct cursors. Pass `nextCursor` back exactly as received.

### Response

```json
{
  "items": [
    {
      "id": "2eb1dbef-94a4-4d6d-a6f1-d813cd45fa60",
      "serial": "WO-001245",
      "lifecycle": "closed",
      "odooStatus": "not_entered",
      "updatedAt": "2026-07-29T18:12:45.000Z"
    }
  ],
  "nextCursor": null
}
```

Ordering is deterministic by `updatedAt`, then `id`, both descending.

### Queue rules

- `pending` returns `closed` workorders whose Odoo state is `not_entered`.
- `missing_info` returns eligible workorders requiring office correction.
- `entered` returns successfully handed-off workorders for reconciliation.
- Active or mechanic-only work is never returned.

## 6. Read a workorder

```http
GET /workorders/{workorderId}
```

`workorderId` is the product UUID returned by the list endpoint.

### Response

```json
{
  "id": "2eb1dbef-94a4-4d6d-a6f1-d813cd45fa60",
  "companyId": "8f84d529-a70a-4ea4-9c93-70ff7336a756",
  "serial": "WO-001245",
  "lifecycle": "closed",
  "odooStatus": "not_entered",
  "serviceOrderNo": "",
  "externalId": "",
  "concern": "Inspect and repair coolant leak",
  "diagnosis": "Upper hose split near clamp",
  "workPerformed": "Replaced upper coolant hose and pressure tested",
  "officeNotes": "Approved for Odoo entry",
  "customer": {
    "displayName": "Example Fleet LLC"
  },
  "workDates": {
    "start": "2026-07-29",
    "end": "2026-07-29"
  },
  "odometer": "482150",
  "location": {
    "id": "52d456cb-e131-44a7-87ad-37eddd1327d1",
    "name": "Chino Yard",
    "type": "yard",
    "address": "Chino, CA"
  },
  "asset": {
    "id": "69898f4f-9b6b-4c7f-83c0-61de1cb1d671",
    "unitNo": "182",
    "name": "Truck 182",
    "unitType": "Truck",
    "vin": "1FUJGLDR0GLGX0001",
    "licensePlate": "9ABC123",
    "make": "Freightliner",
    "model": "Cascadia",
    "year": 2016,
    "lastOdometerMiles": 482150,
    "ownerName": "Example Fleet LLC"
  },
  "mechanics": [
    {
      "id": "4ec77867-f153-429c-b59d-fc647d28c7d8",
      "name": "Chino Mechanic 1",
      "assignmentRole": "primary",
      "assignedAt": "2026-07-29T16:35:00.000Z"
    }
  ],
  "parts": [
    {
      "partNo": "HOSE-9081",
      "qty": "1",
      "uomCode": "pc",
      "repairOrder": "Replace upper coolant hose"
    }
  ],
  "closedAt": "2026-07-29T18:10:00.000Z",
  "createdAt": "2026-07-29T16:26:05.000Z",
  "updatedAt": "2026-07-29T18:12:45.000Z"
}
```

The payload intentionally excludes chat, internal email addresses, authentication identity, session data, raw provider payloads, and unrelated timeline events.

## 7. Record an integration result

```http
PUT /workorders/{workorderId}/result
Authorization: Bearer <integration-token>
Content-Type: application/json
Idempotency-Key: <16-to-200-character-stable-key>
```

### Successful handoff

```json
{
  "status": "entered",
  "serviceOrderNo": "SO-10482",
  "externalId": "odoo:maintenance.request:7812",
  "note": "Created by Odoo integration"
}
```

Required fields:

- `status` must be `entered`;
- `serviceOrderNo` is required, trimmed, and limited to 120 characters;
- `externalId` is required, trimmed, and limited to 300 characters; and
- `note` is optional and limited to 1,000 characters.

### Missing information

```json
{
  "status": "missing_info",
  "note": "Customer billing account mapping is missing."
}
```

`note` is required and must explain what office users need to correct.

### Response

```json
{
  "workorderId": "2eb1dbef-94a4-4d6d-a6f1-d813cd45fa60",
  "status": "entered",
  "serviceOrderNo": "SO-10482",
  "externalId": "odoo:maintenance.request:7812",
  "note": "Created by Odoo integration",
  "updatedAt": "2026-07-29T18:20:00.000Z"
}
```

The product writes lifecycle, Odoo state, attention state, external mapping, audit event, outbox event, and idempotency response in one database transaction.

## 8. Idempotency and retries

Every `PUT` requires an `Idempotency-Key`.

Build the key from a durable Odoo job or request identifier:

```text
odoo-workorder-result:<product-workorder-uuid>:<odoo-job-uuid>
```

Rules:

- The key is scoped to one integration client.
- The key is bound to the HTTP method, path, and request body.
- Replaying the same key and request returns the original successful response.
- A replayed response includes `Idempotency-Replayed: true`.
- Reusing a key with a different path or body returns `409`.
- Preserve the same key when retrying a timeout or `5xx`.
- Generate a new key only for a new logical result.

Recommended retry policy:

| Condition | Action |
| --- | --- |
| Network timeout | Retry with the same key using exponential backoff. |
| `429` | Wait for `Retry-After`, then retry with the same key. |
| `500` | Retry with the same key using exponential backoff. |
| `400`, `403`, `404`, `409`, `413`, `415` | Do not retry unchanged; correct the request or mapping. |

## 9. Error contract

Authentication, authorization, validation, resource, and workflow errors use this envelope:

```json
{
  "error": {
    "code": "WORKORDER_NOT_ELIGIBLE",
    "message": "Office approval is required before Odoo processing.",
    "requestId": "req_01J..."
  }
}
```

Validation errors may also include `error.details`.

Request-parser, rate-limit, and unexpected server errors currently use the legacy flat envelope:

```json
{
  "error": "Too many requests. Try again later.",
  "code": "rate_limit_exceeded"
}
```

The Odoo client must accept both envelope shapes throughout API v1.

| HTTP status | Meaning |
| --- | --- |
| `400` | Invalid cursor, JSON, query parameter, idempotency key, or result body. |
| `401` | Missing, malformed, expired, revoked, or incorrect machine token. |
| `403` | Token does not include the required scope. |
| `404` | Workorder is absent, outside the token's company, or not eligible for exposure. |
| `409` | Workorder state or idempotency conflict. |
| `413` | Request body exceeds the server limit. |
| `415` | Request body is not `application/json`. |
| `429` | Integration rate limit exceeded. |
| `500` | Unexpected server failure. |

Always log `requestId` with the Odoo job record. Do not log the bearer token.

## 10. Field mapping rules

Use product UUIDs as immutable external keys. Human-readable serial numbers and labels are not durable identities.

| Product field | Meaning | Recommended Odoo target |
| --- | --- | --- |
| `id` | Product workorder UUID | Custom unique external-reference field |
| `serial` | Human-facing workorder number | Service-order reference |
| `companyId` | Product company UUID | Approved Odoo company mapping |
| `location.id` | Repair location UUID | Warehouse/shop mapping |
| `customer.displayName` | Historical customer or owner snapshot | Partner display snapshot |
| `asset.id` | Product asset UUID | Fleet/equipment external key |
| `asset.unitNo` | Fleet unit number | Vehicle/equipment identifier |
| `asset.vin` | Vehicle VIN | Vehicle VIN |
| `odometer` | Workorder odometer snapshot | Service-order odometer |
| `concern` | Requested work | Request/complaint |
| `diagnosis` | Mechanic diagnosis | Diagnosis field |
| `workPerformed` | Completed repair | Resolution/work performed |
| `workDates.start`, `workDates.end` | Work date range | Scheduled/completed dates |
| `mechanics[].id` | Product mechanic UUID | Approved employee mapping |
| `parts[].partNo` | Part-number snapshot | Product/SKU mapping |
| `parts[].qty` | Decimal quantity serialized as text | Parsed decimal quantity |
| `parts[].uomCode` | Product unit code | Approved Odoo UoM mapping |
| `parts[].repairOrder` | Work performed for the part row | Service-order line description |

### Customer rule

`customer.displayName` is a historical customer or unit-owner snapshot. It is not the product company and is not the repair location. Do not automatically create an Odoo partner from this text without an approved mapping policy.

### Quantity and UoM rule

- Parse `qty` as decimal text, never binary floating point.
- Count and packaging units require whole-number quantities.
- Packaging conversions are product-specific.
- Never assume all cases, boxes, jugs, drums, or cylinders share one conversion.
- If no safe product or UoM mapping exists, report `missing_info`; do not guess.

## 11. Polling and reconciliation

Recommended initial polling:

- poll `status=pending` every 1 to 5 minutes;
- follow `nextCursor` until it is `null`;
- process the oldest item from the collected backlog first;
- fetch detail immediately before writing to Odoo;
- reconcile `status=entered` daily; and
- store the product workorder UUID uniquely in Odoo.

Before creating an Odoo order, look up the product workorder UUID in Odoo. This prevents duplicates if a prior Odoo write succeeded but the confirmation request timed out.

## 12. Security checklist

- Use HTTPS in every non-local environment.
- Store the bearer token only in a secret manager.
- Keep separate clients for test and production.
- Grant only `workorders:read` and `workorders:write`.
- Rotate and revoke tokens from the Admin integration page.
- Never use a human login or browser cookie for server integration.
- Never log credentials, authorization headers, or full workorder payloads.
- Treat `requestId`, product workorder UUID, and Odoo job UUID as safe correlation identifiers.

## 13. Acceptance checklist

- [ ] Odoo stores the product workorder UUID in a unique indexed field.
- [ ] The worker authenticates with a company-scoped machine client.
- [ ] Pending pagination follows every cursor without omissions.
- [ ] Active and cross-company workorders cannot be read.
- [ ] Partner, asset, mechanic, product, and UoM mappings are explicit.
- [ ] Unknown mappings report `missing_info`.
- [ ] Successful results include both `serviceOrderNo` and `externalId`.
- [ ] Retries preserve the same idempotency key.
- [ ] Replayed requests do not create duplicate Odoo orders.
- [ ] `requestId` is retained for support investigations.
- [ ] No browser cookie, password, token, or full payload appears in logs.
- [ ] A staging run covers success, missing mapping, replay, conflict, revocation, and Odoo downtime.

## 14. Verified implementation behavior

The local acceptance run verified:

- unauthenticated requests return `401`;
- a valid client lists its own pending workorders;
- open workorders are hidden;
- cross-company workorders are hidden as `404`;
- detail payloads exclude chat and authentication data;
- `missing_info` creates office attention state;
- identical idempotent replay returns the original success;
- changed input with the same key returns `409`;
- `entered` creates the external mapping and reconciliation state;
- audit, outbox, mapping, and idempotency records are written; and
- revoking the machine client immediately causes subsequent requests to return `401`.

The machine-readable contract is `ODOO_INTEGRATION_TARGET.openapi.yaml`.
