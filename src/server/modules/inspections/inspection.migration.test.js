import test from "node:test";
import assert from "node:assert/strict";
import { readFile } from "node:fs/promises";
const migrations=new URL("../../db/migrations/",import.meta.url);
test("inspection migrations constrain weekly kinds, optimistic state, immutable events, and archive lineage",async()=>{const domain=await readFile(new URL("098_inspections.sql",migrations),"utf8");const archive=await readFile(new URL("099_inspection_print_history.sql",migrations),"utf8");assert.match(domain,/inspection_kind in \('weekly_truck', 'weekly_trailer'\)/);assert.doesNotMatch(domain,/annual|fmcsa|periodic/i);assert.match(domain,/version bigint not null default 1/);assert.match(domain,/inspection_events_append_only/);assert.match(archive,/source_provider = 'local_inspection'/);assert.match(archive,/predecessor_archive_id/);assert.match(archive,/inspection_print_archive_immutable/);});
test("product and template migrations use sparse exact modes and immutable published versions",async()=>{const access=await readFile(new URL("096_product_module_access.sql",migrations),"utf8");const templates=await readFile(new URL("097_template_platform.sql",migrations),"utf8");assert.match(access,/access_mode in \('off', 'read', 'full'\)/);assert.match(access,/location_id/);assert.match(access,/user_id/);assert.match(templates,/protect_published_template_version/);assert.match(templates,/template_assignments_location_override_uidx/);});
test("inspection substring search has trigram index support",async()=>{const search=await readFile(new URL("101_inspection_search_indexes.sql",migrations),"utf8");assert.match(search,/create extension if not exists pg_trgm/i);assert.match(search,/inspection_number gin_trgm_ops/i);assert.match(search,/asset_snapshot->>'unitNo'\) gin_trgm_ops/i);assert.match(search,/asset_snapshot->>'vin'\) gin_trgm_ops/i);assert.match(search,/asset_snapshot->>'licensePlate'\) gin_trgm_ops/i);});
test("assignment-history upgrade reconciles legacy duplicates in an ordered migration without rewriting migration 100",async()=>{const cleanup=await readFile(new URL("099_inspection_assignment_reconciliation.sql",migrations),"utf8");const constraint=await readFile(new URL("100_inspection_assignment_history.sql",migrations),"utf8");assert.match(cleanup,/delete from inspection_assignments superseded/);assert.match(cleanup,/superseded\.active = false/);assert.match(cleanup,/current\.active = true/);assert.match(constraint,/add constraint inspection_assignments_one_row_per_mechanic/);assert.doesNotMatch(constraint,/delete from inspection_assignments/);});
